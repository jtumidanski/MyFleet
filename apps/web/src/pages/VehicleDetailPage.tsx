import { PlaceholderPage } from './PlaceholderPage';

// Placeholder — replaced with the canonical vehicle detail page in Task 14.3.
export function VehicleDetailPage() {
  return <PlaceholderPage title="Vehicle" />;
}
