import { PlaceholderPage } from './PlaceholderPage';

// Placeholder — replaced with the canonical vehicles feature in Task 14.3.
export function VehiclesPage() {
  return <PlaceholderPage title="Vehicles" />;
}
