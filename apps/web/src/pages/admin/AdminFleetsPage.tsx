/**
 * Placeholder for the admin Fleets screen. The route tree is wired up front so
 * the shell and its guard can be tested as a unit; the screen itself lands in
 * its own task.
 */
export function AdminFleetsPage() {
  return null;
}
