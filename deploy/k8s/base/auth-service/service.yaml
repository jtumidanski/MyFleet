apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: myfleet
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
