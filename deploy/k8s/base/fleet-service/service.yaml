apiVersion: v1
kind: Service
metadata:
  name: fleet-service
  namespace: myfleet
  labels:
    app: fleet-service
spec:
  type: ClusterIP
  selector:
    app: fleet-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
