apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: myfleet
  labels:
    app: media-service
spec:
  type: ClusterIP
  selector:
    app: media-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
