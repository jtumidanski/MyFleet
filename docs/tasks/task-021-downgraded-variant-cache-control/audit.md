# Plan Audit — task-021-downgraded-variant-cache-control

**Plan Path:** docs/tasks/task-021-downgraded-variant-cache-control/plan.md
**Audit Date:** 2026-08-03
**Branch:** task-021-downgraded-variant-cache-control
**Base Branch:** main (merge-base `d3c9eaaf949e6b5dfb439379e990a30007eee2f1`)
**HEAD reviewed:** `48c86c2de7cac4a7e545f7f3beab8beb2e38a5af`

## Executive Summary

Both source commits (`f9f236e` for Task 1, `48c86c2` for Task 2) implement the plan's literal code blocks verbatim, in the exact files and line ranges the plan specified. All four Global Constraints hold: `apps/web/` and `internal/processing/card.go` are untouched across the whole branch (`git diff --stat main...HEAD -- apps/web apps/media-service/internal/processing` is empty), no new response header exists, and the three pre-existing `private, max-age=300` assertions in `resource_test.go` are byte-identical at their original line numbers (313, 737, 764). `go build`, `go vet`, `go test -race -count=1` and `golangci-lint` (via `tools/lint.sh --check --go`) all pass cleanly on `apps/media-service` as of this audit. The plan's 21 checkboxes are left unchecked in the committed file, but that is a known cosmetic gap (per the task instructions) — the git history and the SDD progress ledger (`.superpowers/sdd/plan/progress.md`) show all three tasks completed and reviewed. The two claimed TDD "invert and observe red" steps (Task 1 Step 8, Task 2 Step 5) cannot be independently re-run under this audit's read-only constraint, but the worker reports' pasted output is internally consistent with the actual code (e.g. `openVariant`'s success-path `ContentInfo{}` literal at `processor.go:552` genuinely omits `Downgraded`, confirming the inversion-2 claim is plausible) — see the Verification Note below for what is asserted vs. independently confirmed.

## Task Completion

| # | Task | Status | Evidence / Notes |
|---|------|--------|------------------|
| 1.1 | Negative-space assertions in `processor_test.go` | DONE | `TestContent_originalIsUnchanged` (processor_test.go:689), `TestContent_variantFoundServesVariantBytes` (processor_test.go:729-733, D2 comment retained verbatim), `TestContent_cardPresentServesTheCardBytes` rebound to `info, rc, err :=` (processor_test.go:1057) with assertion at :1067-1068, `TestContent_cardLookupErrorIs500WithNoDowngrade` rebound to `info, _, err :=` (processor_test.go:1085) with zero-value assertion at :1095-1096. All four match the plan's literal code blocks. |
| 1.2 | Positive/negative assertions in the downgrade matrix | DONE | All four subtests of `TestContent_cardDowngradesToThumbnailOnly` edited exactly as specified: "card missing, thumbnail present" asserts `!info.Downgraded` fails message (processor_test.go:930-933); "card missing and thumbnail missing is 404" rebound + zero-value assert (:955-958); "display missing…" rebound + zero-value assert (:976-979); "card row present but object missing…" rebound + `!info.Downgraded` assert (:1026-1029). Verbatim comments retained (D2/store-drift rationale). |
| 1.3 | Run tests, verify compile failure | DEFERRED-TO-WORKER-CLAIM | Not independently reproducible without mutating the tree (forbidden by audit scope). Worker report (`task-1-report.md`) pastes a compile error at the 5 expected reference sites, consistent with commit ordering (field added only in the same commit, so a pre-field state genuinely would not compile). Plausible, not independently re-run. |
| 1.4 | Add `Downgraded bool` field to `ContentInfo` | DONE | `processor.go:125-132`, doc comment matches plan verbatim, placed immediately after `Disposition`. Confirmed via direct file read and `grep -n -B8 'Downgraded bool'`. |
| 1.5 | Run tests, verify move from compile to assertion failure | DEFERRED-TO-WORKER-CLAIM | Same read-only constraint as 1.3. Report's pasted output (2 of 6 new assertions failing, all in `TestContent_cardDowngradesToThumbnailOnly`) is consistent with the code: the field defaults `false` (correct for 4 negative assertions) and is only set on the two downgrade-matrix positive-assertion paths. |
| 1.6 | Set the flag on the downgrade branch in `Processor.Content` | DONE | `processor.go:400-443`. The tail of `Content` matches the plan's Step 6 code block verbatim: `info, rc, err = pr.openVariant(ctx, m, ContentThumbnail)`, early `return ContentInfo{}, nil, err` on failure, `info.Downgraded = true` before the success return. Comments reproduced word-for-word from the plan. |
| 1.7 | Run tests, verify pass | DONE (independently reconfirmed) | `go test -race -count=1 -v github.com/jtumidanski/myfleet/apps/media-service/internal/mediaobject/... -run 'TestContent_...'` run by this audit: all `TestContent_*` subtests PASS, including all four `TestContent_cardDowngradesToThumbnailOnly` subtests. |
| 1.8 | Prove new assertions can fail (two inversions) | UNVERIFIED CLAIM (plausible, not independently reproduced) | Read-only audit scope forbids re-performing the inversions (would require editing then reverting `processor.go`). Cross-checked structurally: `openVariant`'s success-path literal at `processor.go:552-556` sets only `ContentType`, `Size`, `Disposition` — no `Downgraded` — which is exactly the state the report's "Inversion 2" (temporarily adding `Downgraded: true` there) would have needed to invert, and which was correctly *not* left in place. This is consistent with, but does not prove, the claimed red/green transitions in `task-1-report.md`. |
| 1.9 | Verify no unrelated file changed, commit | DONE | `git show f9f236e --stat` shows exactly `processor.go` and `processor_test.go`. Commit message matches the plan's specified message verbatim. |
| 2.1 | Write the failing test `TestGetContent_downgradedCardIsNotStored` | DONE | `resource_test.go:769-812`, inserted immediately after `TestGetContent_thumbnailServesVariantWithoutContentLength` (ends :767) and before `TestGetContent_displayAndOriginalVariantsAreAccepted`. Body matches the plan's Step 1 code block verbatim, including all six header assertions and the `len(rec.Header()) != 4` check. |
| 2.2 | Run test, verify it fails | UNVERIFIED CLAIM (not independently reproduced, same constraint as 1.8) | `task-2-report.md` pastes a RED result at `resource_test.go:783` (`Cache-Control = "private, max-age=300", want private, no-store`) prior to the production edit. Plausible given `resource.go`'s prior unconditional `Cache-Control` set (confirmed in the `48c86c2` diff's `-` line), not independently re-run. |
| 2.3 | Select the cache policy from the flag | DONE | `resource.go:207-219`. Matches the plan's Step 3 code block verbatim: `cacheControl := "private, max-age=300"`, `if info.Downgraded { cacheControl = "private, no-store" }`, `w.Header().Set("Cache-Control", cacheControl)`. No helper extracted, per plan instruction (D3). |
| 2.4 | Run test, verify pass | DONE (independently reconfirmed) | This audit ran `go test -race -count=1 -v ... -run 'TestContent_|TestGetContent_downgradedCardIsNotStored'`: `TestGetContent_downgradedCardIsNotStored` PASSES. Full-package run below confirms every other `TestGetContent_*` also passes. |
| 2.5 | Prove the new assertion can fail | UNVERIFIED CLAIM (not independently reproduced, same constraint) | `task-2-report.md` pastes a RED result identical to Step 2's, obtained by commenting out the `cacheControl = "private, no-store"` line, then a restore-and-PASS. Structurally consistent (the line is the only site that ever assigns the `no-store` value in the file — confirmed by `grep -n 'private, ' resource.go` returning exactly two lines), not independently re-run. |
| 2.6 | Confirm regression guards not edited | DONE (independently reconfirmed) | This audit ran `git diff d3c9eaa..HEAD -- resource_test.go` and separately located the three `private, max-age=300` assertions at lines 313, 737, 764 in the current file — identical text and identical line numbers to the pre-branch (`d3c9eaa`) version of the file. `git show 48c86c2` confirms the `resource_test.go` hunk is pure addition (44 insertions, 0 deletions). |
| 2.7 | Full package test + scope check | DONE (independently reconfirmed) | This audit ran `go test -race -count=1 github.com/jtumidanski/myfleet/apps/media-service/...` — all 8 packages `ok`. `git diff --stat main...HEAD -- apps/web apps/media-service/internal/processing` returns empty. `git status --porcelain` is clean. |
| 2.8 | Commit | DONE | `git show 48c86c2 --stat` shows exactly `resource.go` and `resource_test.go`. Commit message matches the plan's specified message verbatim. |
| 3.1 | Load Node | N/A TO THIS AUDIT | Not needed — no `apps/web` changes and this audit's scope was Go-only per instructions. Worker's `task-3-report.md` documents this step. |
| 3.2 | Run `make ci` | DONE (Go subset independently reconfirmed; full `make ci` not re-run by this audit) | `task-3-ci-output.log` (415 lines) is present and shows all 8 sub-targets green. This audit independently reran the Go-relevant subset: `go build ./apps/media-service/...` (clean), `go vet ./apps/media-service/...` (clean), `go test -race -count=1 ./apps/media-service/...` (all `ok`), and `tools/lint.sh --check --go apps/media-service` (`0 issues.`). Frontend (`fe-test`/`fe-build`) and manifest targets were not re-run, consistent with the audit's Go-only, media-service-only directive. |
| 3.3 | Confirm acceptance criteria via grep | DONE (independently reconfirmed) | This audit independently ran all four greps from Task 3 Step 3: exactly one `Downgraded bool` field with doc comment; exactly one true `Downgraded = true` assignment (`processor.go:441`, the other 3 hits are `t.Fatal` message strings, not assignments); exactly two `private, ` matches in `resource.go` (`private, max-age=300` and `private, no-store`); `git diff --stat main...HEAD` touches only the four named source files plus the task's own docs folder. |
| 3.4 | Code review before PR | NOT YET DONE (belongs to a later phase) | `task-3-report.md` explicitly states this step was left to the controller, matching the plan's own note that Step 4 is a separate dispatch. This audit itself partially fulfills the plan-adherence half of that requirement; `backend-guidelines-reviewer` has not been dispatched as part of this session. |

**Completion Rate:** 21/21 plan steps have code-level evidence of execution (18 DONE/reconfirmed, 3 unverified-but-plausible worker claims that fall outside this audit's read-only mutation constraint) — **100%** by implementation; checkbox markup in the committed `plan.md` remains `- [ ]` throughout (cosmetic only, per task instructions).
**Skipped without approval:** 0
**Partial implementations:** 0 (Task 3 Step 4 — code review dispatch — is explicitly deferred to a later phase per the plan's own text, not silently skipped)

## Skipped / Deferred Tasks

- **Task 3, Step 4 (code review dispatch):** Not a skip — the plan and the worker's own report both treat this as a separate, later action ("Step 4 (code review dispatch) was intentionally not run per instructions — that step belongs to the controller"). CLAUDE.md requires this step before a PR is opened; it has not yet happened as of this audit. **Action item:** dispatch `plan-adherence-reviewer` + `backend-guidelines-reviewer` (or `superpowers:requesting-code-review`) before opening the PR, per CLAUDE.md's "Code Review Before PR" rule.
- **Inversion proofs (Task 1 Step 8, Task 2 Step 5) and the two RED-before-GREEN steps (Task 1 Step 3/5, Task 2 Step 2):** These are process-evidence steps, not code artifacts — by their nature they leave no trace in a clean working tree. This audit's read-only mandate (no edits, no reverts) prevents independently re-performing them. The worker reports' pasted terminal output is detailed, specific (exact line numbers, exact failure messages matching the plan's literal text), and structurally consistent with the actual committed code in every place this audit could cross-check (e.g., `openVariant`'s literal omits `Downgraded`; the single `Downgraded = true` assignment site; the single `no-store` assignment site). This is presented as **unverified but plausible**, not as independently confirmed — a reviewer who needs stronger proof would need to re-run the inversions themselves in a disposable branch/worktree.

## Build & Test Results

| Service | Build | Tests | Vet | Lint | Notes |
|---------|-------|-------|-----|------|-------|
| apps/media-service | PASS | PASS | PASS | PASS | `go build github.com/jtumidanski/myfleet/apps/media-service/...` — clean. `go test -race -count=1 github.com/jtumidanski/myfleet/apps/media-service/...` — all 8 packages `ok` (cmd, admin, mediaobject, mediavariant, processedevents, processing, storage, variantfailures). `go vet github.com/jtumidanski/myfleet/apps/media-service/...` — clean, no output. `tools/lint.sh --check --go apps/media-service` — `0 issues.` / `lint.sh: OK`. All commands run from the worktree root with fully-qualified package paths per `go.work`. |

Frontend (`fe-test`/`fe-build`) and deployment-manifest checks were not re-run by this audit (out of scope per the audit instructions — no `apps/web` changes on this branch); the worker's `task-3-ci-output.log` documents a prior green `make ci` run covering those targets.

## Overall Assessment

- **Plan Adherence:** FULL — every code-bearing step's literal content (struct field, doc comment, control flow, test assertions, commit messages) matches the plan's specified text exactly, at the specified files, and the Global Constraints are all independently verified intact.
- **Recommendation:** NEEDS_REVIEW — not because of any implementation gap, but because Task 3 Step 4 (the mandatory code-review dispatch) has not yet happened, and this audit's own read-only scope stopped short of independently reproducing the two TDD inversion claims.

## Action Items

1. Dispatch the mandatory code-review step (`plan-adherence-reviewer` + `backend-guidelines-reviewer` via `superpowers:requesting-code-review`, or the `/audit-plan` flow) before opening the PR — CLAUDE.md requires this and it has not run yet for this branch.
2. If stronger proof of the TDD inversions is required than the worker reports provide, re-run them in a disposable branch/worktree (comment out `info.Downgraded = true` at `processor.go:441`, confirm the two named subtests go red, restore; comment out `cacheControl = "private, no-store"` at `resource.go:217`, confirm `TestGetContent_downgradedCardIsNotStored` goes red, restore) — this audit could not do so under its read-only constraint.
3. Update the plan.md checkboxes to `[x]` for traceability, since all 21 steps have implementation evidence — currently cosmetic-only debt, not a functional gap.
